
# Playwright
node_modules
node_modules/
.env

# macOS
.DS_Store

# Claude Code yerel ayarları
.claude/settings.local.json
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
/playwright/.auth/

# WP-R2 rapor artifact'leri — HTML/PDF Git'e commit EDİLMEZ (MD/JSON kaynak-of-truth commit'lenir)
/docs/raporlar/*.html
/docs/raporlar/*.pdf
# FAZ 2 trend geçmişi — canlı koşum ürünü snapshotları; commit edilmez (drift'siz artifact, cross-run persistence FAZ 3)
/docs/raporlar/history/
e.env/
